import 'package:flutter/material.dart';

class PracticeEndPage extends StatelessWidget {
  const PracticeEndPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Procvičeno xx hádanek'),
      ),
    );
  }
}
